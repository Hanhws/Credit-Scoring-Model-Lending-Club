# Lending Club 신용평가 모델 — 결과 가이드

이 문서는 `결과/`, `시각화/` 폴더에 있는 모든 표와 그래프가 **무엇을 나타내는지, 왜 그렇게 계산했는지, 어떤 결론을 뜻하는지**를 프로젝트를 처음 보는 사람도 이해할 수 있도록 정리한 것입니다.

---

## 1. 무엇을 하는 프로젝트인가

Lending Club(미국 P2P 대출 플랫폼)의 대출 데이터를 이용해서, **"어떤 대출을 승인해줘야 위험 대비 수익이 좋은가"**를 판단하는 신용평가 모델을 만들고, 그 성과를 **Sharpe ratio / Sortino ratio**(위험조정수익률)로 평가하는 프로젝트입니다.

핵심 질문은 두 가지입니다.
1. 대출을 골라 투자했을 때, **평균적으로 얼마나 버는가** (수익률)
2. 그 수익이 **시기마다 얼마나 들쭉날쭉한가** (위험/변동성)

이 두 가지를 하나의 숫자로 합친 것이 Sharpe/Sortino ratio이고, 이 프로젝트는 "모델이 대출을 골라줬을 때 이 숫자가 좋아지는가"를 검증합니다. 여러 모델 후보를 비교했고, 최종적으로는 **"대출 선별 기준(K)을 어떻게 정하느냐"에도 명확한 경제적 근거**를 두도록 다듬었습니다.

---

## 2. 사용한 데이터

| 파일 | 내용 |
|---|---|
| `data/raw/lending_club_2020_train.csv` | 학습용 대출 데이터 (약 175만 건) |
| `data/raw/lending_club_2020_test.csv` | 최종 평가용 대출 데이터 (약 117만 건, 학습에는 전혀 사용 안 함) |
| `data/raw/DGS3.csv` / `DGS5.csv` | 미국 3년물 / 5년물 국채 수익률 (FRED 제공) — "무위험 수익률" 기준으로 사용 |

Lending Club 대출은 만기가 **36개월(3년) 또는 60개월(5년)** 두 종류뿐입니다. 그래서 36개월 대출은 3년물 국채(DGS3), 60개월 대출은 5년물 국채(DGS5) 수익률과 비교합니다 — "이 돈을 대출 대신 국채에 넣었으면 얼마 벌었을까"라는 기회비용을 만기에 맞춰 공정하게 비교하기 위해서입니다.

데이터 마지막 시점(스냅샷)은 대략 **2020년 12월**입니다 (대출별 마지막 결제일/신용조회일 중 가장 늦은 값 기준).

---

## 3. 핵심 개념 정의 (계산식)

### 3-1. 왜 "완료된(matured)" 대출만 쓰는가

`loan_status`가 `Current`(상환 중), `Late`(연체), `In Grace Period`(유예기간) 등인 대출은 **아직 결과를 모릅니다** (완전 상환할지, 부도날지 미정). 이런 대출은 수익률 계산에서 아예 제외하고, 결과가 확정된 대출만 사용합니다:
- `Fully Paid` (완전 상환)
- `Charged Off` (부도 처리)
- `Default` (부도)
- 정책 미달 상환/부도 대출 2종

### 3-2. 중요한 편향 하나 더: "최근 대출일수록 빨리 끝난 것만 보인다" 문제

여기서 그치면 안 되는 이유가 있습니다. 예를 들어 2019년에 나간 60개월 만기 대출은, 2020년 12월 스냅샷 시점에서 아직 1년 남짓밖에 안 지났기 때문에, **정상적으로 몇 년째 잘 갚고 있는 대출은 전부 "Current" 상태라 제외**되고, 오직 **아주 빨리 부도나거나 아주 빨리 다 갚아버린** 대출만 "matured" 표본에 남습니다. 이 상태로 계산하면 최근 발행월일수록 실제보다 훨씬 나빠 보이는 착시가 생깁니다.

그래서 **"발행월 + 만기가 스냅샷 시점(2020-12) 이전에 이미 다 지난 대출"만** 사용하도록 한 번 더 걸렀습니다. 이 필터 때문에 60개월 대출은 2015년 12월 이전 발행분까지만, 36개월 대출은 2017년 12월 이전 발행분까지만 분석에 포함됩니다. (필터 적용 후 학습 데이터는 약 88만 건, 평가 데이터는 약 58만 건)

### 3-3. 수익률(R), 무위험수익률(Rf), 초과수익률(ER)

대출 1건에 대해:

```
R (누적 실현수익률) = (총 상환액 − 대출원금) / 대출원금
```
예: 1,000만원 빌려주고 총 1,080만원을 돌려받았다면 R = 8%. 부도로 300만원만 돌려받았다면 R = -70%.

```
Rf (무위험수익률, 연율) = 대출 발행월의 만기매칭 국채수익률 (36개월→DGS3, 60개월→DGS5)
Rf_cum (누적 무위험수익률) = Rf × 실제 보유기간(년)
```
여기서 "실제 보유기간"을 쓰는 게 중요합니다. 명목 만기(3~5년)로 계산하면, 몇 달 만에 조기상환된 대출을 "5년치 국채수익률"과 비교하는 오류가 생겨서 실제보다 훨씬 나빠 보이게 됩니다. (이 프로젝트 진행 중 실제로 이 버그를 발견해서 수정했습니다.)

```
ER (초과수익률) = R − Rf_cum
```
ER이 양수면 "국채보다 나은 투자", 음수면 "국채만도 못한 투자"라는 뜻입니다. **모델이 예측하는 값이 바로 이 ER입니다.**

### 3-4. Sharpe ratio / Sortino ratio

개별 대출이 아니라, **"코호트"(같은 달에 발행된 대출 묶음, vintage)** 단위로 계산합니다.

1. 각 코호트마다 대출금액(달러)을 먼저 다 더해서 "그 달 전체가 실제로 얼마 벌었는지"를 구합니다 (개별 대출 건수가 아니라 **투자금액 기준 가중** — $40,000짜리 대출 부도가 $1,000짜리 부도보다 40배 크게 반영됨).
2. 그렇게 나온 코호트별 초과수익률(ER)의 **시간에 따른 흐름**을 만듭니다. (→ `시각화/cohort_excess_return.png`가 바로 이 흐름)
3. 이 흐름의 평균과 변동성으로 계산합니다.

```
Sharpe  = 코호트별 ER의 평균 / 코호트별 ER의 표준편차
Sortino = 코호트별 ER의 평균 / 코호트별 ER의 "하방편차"(평균보다 나쁜 코호트들만으로 계산한 변동성)
```

**Sharpe와 Sortino의 차이**: Sharpe는 수익이 평균보다 좋을 때(상방)와 나쁠 때(하방)를 똑같이 "위험"으로 취급합니다. 반면 대출은 손실이 원금까지로 제한되고(최대 -100%) 이익은 이자율만큼으로 제한되는(상방도 제한적) 비대칭 구조라, **나쁜 쪽 변동성만 벌점을 주는 Sortino가 이 도메인에는 더 적합**하다고 보고, 모델을 고르고 튜닝하는 기준(메인 지표)으로는 Sortino를 사용했습니다. 다만 과제 평가기준으로 지정된 것은 Sharpe이므로, 보고서에는 두 지표를 함께 제시하는 것을 권장합니다.

### 3-5. 달러 인수율 (dollar acceptance rate)

이 문서와 결과표에 반복해서 나오는 **"인수율"**은 다음과 같이 계산합니다 (`src/metrics.py`).

```
달러 인수율 = (선택된 대출들의 funded_amnt 합) / (전체 대출들의 funded_amnt 합)
```

세 가지를 정확히 짚어야 합니다.

1. **분모(전체 대출)는 "LC에 신청한 모든 사람"이 아니라 "LC가 이미 심사해서 승인·실행(funded)한 대출 중 결과가 확정된 것들"**입니다. 이 데이터 자체가 LC 승인이 끝난 대출만 담고 있어서, 여기서 말하는 인수율은 "LC의 신청 대비 승인율"이 아니라 **"LC가 이미 승인해준 대출 목록 중에서, 우리 모델/규칙이 투자자로서 얼마를 추가로 골라 투자하겠다고 하는가"**입니다. 즉 이 프로젝트는 대출 심사(승인/거절) 자체가 아니라 **이미 승인된 대출 풀에 대한 투자 배분**을 다룹니다.
2. **건수가 아니라 달러 가중**입니다. 예를 들어 어떤 코호트에 $10,000짜리 4건, $40,000짜리 1건(총 $80,000)이 있을 때 모델이 $40,000짜리 하나만 고르면, 건수 기준으로는 20%지만 **달러 인수율은 50%**입니다. Sharpe/Sortino를 달러가중으로 계산하는 것(3-4절)과 같은 원칙입니다.
3. **어떤 선택 규칙이냐에 따라 계산 대상이 달라집니다**:
   - 그리드서치 K (`top_k_selection`, 7절): 코호트 안에서 예측순위 상위 K%에 해당하는 달러 → K=45%면 그 달 대출금액의 45%가 고정적으로 인수율이 됨
   - **ER_hat>0 규칙 (`threshold_selection`, 최종 채택안, 7~9절)**: 몇 %가 될지 미리 정하지 않고, 예측 초과수익률이 양수인 대출만 선택한 결과로 인수율이 사후적으로 결정됨 — 그래서 `threshold_rule_acceptance.png`에서 시기별로 12%~98%까지 오르내리는 것

---

## 4. 모델 설명 (1차: LightGBM)

### 4-1. 무엇을 예측하는가
**LightGBM 회귀모델**로, 대출 1건의 **ER(초과수익률)**을 예측합니다. (분류가 아니라 회귀 — "부도냐 아니냐"가 아니라 "얼마나 벌 것으로 예상되는가"를 직접 예측)

### 4-2. 어떤 변수를 썼는가 (ex-ante 변수만 사용)
**대출 승인 시점에 이미 알 수 있는 정보만** 사용했습니다. 상환 이후에만 알 수 있는 정보(총 상환액, 연체 이력, 마지막 결제일, hardship/debt settlement 여부 등)는 전부 제외했습니다 — 이런 걸 넣으면 "미래 정보로 미래를 맞추는" 반칙(data leakage)이 되기 때문입니다. 이 변수 목록은 이후 모델 비교(6절)에서도 동일하게 사용했습니다.

사용한 변수 카테고리 (대표 예시):
- **대출 조건**: 대출금액, 만기(36/60개월), 금리, 월상환액, LC자체 등급(grade/sub_grade)
- **차주 정보**: 연소득, 고용기간, 주거형태, 소득증빙상태, 대출목적, 거주 주(state)
- **신용이력**: FICO 점수, 신용거래 개월수, 연체 이력, 신용조회 횟수, 신용한도/사용률 등 신용조회사(credit bureau) 지표 다수

**제외한 변수**: `zip_code`(우편번호)는 처음엔 중요도(gain) 1위로 나왔지만, 실제로 이 변수를 뒤섞어도(permutation test) 예측 정확도가 거의 안 변해서(937개 우편번호 카테고리 중 상당수가 샘플 수 부족) "과적합으로 부풀려진 가짜 중요도"로 판단하고 제거했습니다.

### 4-3. 학습 방법
- 학습 데이터(88만 건)를 85%/15%로 나눠 85%로 학습, 15%로 조기종료(early stopping) 시점 결정
- 목적함수: MAE(L1) — ER 값이 부도 시 매우 크게 벌어질 수 있어 이상치에 덜 민감한 손실함수 사용
- 예측-실제 상관계수: 약 0.15 (개별 대출 단위 예측력은 강하지 않음)

---

## 5. LightGBM 검증 — validation vs 교차검증

모델이 예측한 ER 순위를 이용해서 "코호트 안에서 예측이 좋은 상위 K%만 투자한다"는 전략을 만들고, K를 얼마로 할 때 Sortino가 가장 좋은지 찾았습니다.

1. **단일 validation split 방식**: 학습 데이터의 15% 검증셋에서 K를 스윕(5%~100%)한 결과 K*=0.90(상위 90%, 즉 하위 10%만 제외)이 가장 좋았습니다 (Sortino 2.13).
2. **5-fold 교차검증 방식**: 데이터를 5등분해서 5번 반복 검증한 결과, **같은 K값에서도 fold마다 Sortino가 크게 요동쳤고**(예: K=5%에서 fold별로 0.04~3.17까지 차이), 평균적으로는 **K*=1.00(아예 필터링 안 함)이 가장 안정적으로 좋았습니다.

**결론**: 단일 validation split에서 나온 "K=90%가 최적"이라는 결과는 실제로는 우연(노이즈)이었고, 더 엄밀한 교차검증으로 확인해보니 **LightGBM을 상위 K% 선별 필터로 쓰는 방식은 어떤 K에서도 신뢰할 만한 개선을 주지 못합니다.** 테스트셋에서도 LightGBM은 Grade A-C(Sortino 2.19)를 못 넘었습니다(Sortino 1.47).

---

## 6. 다른 모델로 바꾸면 결과가 달라지는가 — 5개 모델 비교

우수사례 보고서들에서 사용한 모델 계열(Linear Regression, Random Forest, XGBoost, CatBoost)을 LightGBM과 **완전히 동일한 변수·동일한 타깃(ER)·동일한 학습/평가 절차**로 비교했습니다. (SVM도 우수사례에 있었지만, 학습 데이터가 88만 건이라 SVM 회귀는 계산량상 현실적으로 불가능해 제외했습니다.)

### 6-1. 비교 결과 — `결과/모델별_비교표.csv`, `시각화/model_comparison.png`

| 모델 | 예측-실제 상관계수 (test) | 그리드서치로 찾은 K* | test Sharpe | test Sortino |
|---|---|---|---|---|
| Linear Regression | 0.164 | 5% | 1.31 | 2.58 |
| Random Forest | 0.193 | 65% | 1.32 | 4.29 |
| **XGBoost** | **0.221** | 45% | **1.59** | **5.01** |
| CatBoost | 0.149 | 25% | 0.88 | 1.78 |
| LightGBM | 0.149 | 90% | 0.78 | 1.47 |
| (베이스라인) 전량 투자 | - | - | 0.79 | 1.53 |
| (베이스라인) Grade A-C | - | - | 0.97 | 2.19 |

**"예측-실제 상관계수"가 핵심입니다** — 이건 K 튜닝과 무관하게, "모델이 개별 대출의 실제 성과를 얼마나 잘 맞히는가"를 나타내는 순수한 지표입니다. XGBoost(0.221)가 다른 모델(0.15 안팎)보다 뚜렷하게 높습니다. 즉 XGBoost는 우연이 아니라 **실제로 이 데이터에서 더 많은 신호를 뽑아냈다**는 뜻입니다.

### 6-2. XGBoost만 별도로 교차검증 — `시각화/cv_instability_xgb.png`

XGBoost가 가장 유력해 보여서, LightGBM에 했던 것과 똑같이 5-fold 교차검증으로 다시 확인했습니다.

- **fold별 상관계수가 0.207~0.219로 거의 흔들리지 않음** (평균 0.214, 표준편차 0.004) → 신호 자체가 안정적으로 강함
- 5-fold 교차검증이 찾은 최적 K(45%)가, 단일 validation split이 찾은 K(45%)와 **정확히 일치**
- K=20%~65% 구간에서는 5-fold 평균 Sortino가 거의 전부 Grade A-C 베이스라인(2.19)보다 높음

XGBoost의 신호 자체는 이렇게 안정적으로 확인됐지만, **"K=45%"라는 특정 숫자를 채택한 방식 자체에는 여전히 문제가 남습니다** → 7절.

---

## 7. "K를 그리드서치로 찾았다"는 것의 문제, 그리고 더 나은 규칙

### 7-1. 무엇이 문제인가

지금까지의 K 선택 방식은 "5%, 10%, …, 100%를 다 계산해보고 Sortino가 제일 높은 걸 고른다"였습니다. 교차검증으로 재확인은 했지만, 근본적으로 **"왜 하필 45%인가"에 대한 경제적 설명이 없습니다.** 20개 후보 중 제일 잘 나온 값을 보고하는 방식은, 시도한 후보가 많을수록 우연히 좋아 보이는 값이 나올 위험(다중비교 문제)이 있고, 결과를 먼저 보고 기준을 정한 것처럼 보일 수 있습니다.

### 7-2. 대안: 파라미터 없는 규칙

K를 탐색하는 대신, **"모델이 예측한 초과수익률(ER_hat)이 0보다 크면 투자한다"**는 규칙을 세웠습니다. 이건 "이 대출이 국채보다 못 벌 것 같으면 안 빌려준다"는 자명한 투자 논리이고, **탐색할 자유도(K)가 아예 없습니다.** 몇 %가 선택될지는 결과일 뿐, 목표로 정한 게 아닙니다.

실제로 XGBoost에 이 규칙을 적용해 테스트셋에서 확인한 결과, 파라미터 없는 규칙이 그리드서치보다 오히려 더 좋고 투자 규모도 훨씬 컸습니다 (정확한 수치는 era-편향 점검을 거친 뒤인 9절 표 기준입니다).

### 7-3. 왜 이 규칙이 더 나은가 — `시각화/threshold_rule_acceptance.png`

이 규칙은 매달 고정된 비율(예: 항상 상위 45%)을 투자하는 게 아니라, **그 시기 대출 상태에 따라 인수율 자체가 자동으로 움직입니다.**

- 2008~2009년(금융위기 전후) 평균 인수율 **81%**
- 2011~2013년(호황기) 평균 인수율 **97%**

즉 모델이 "지금 상황이 안 좋아 보인다"고 판단하면 자동으로 대출을 덜 해주고, "지금 상황이 좋다"고 판단하면 더 많이 해주는 **경기대응적(counter-cyclical) 리스크 관리**가 자연스럽게 나타납니다. 이는 매달 무조건 상위 45%를 채우는 고정 쿼터보다 "장기적으로 건전한 경영"이라는 이 프로젝트의 원래 목적에 더 부합하는 정책입니다.

**단, 주의할 점**: 그래프를 보면 2007년~2008년 초는 인수율이 매우 낮고(12~40%) 들쭉날쭉한데, 이 시기는 Lending Club이 막 시작한 때라 월별 대출 건수 자체가 12~173건으로 매우 적습니다. 이 구간의 급격한 등락은 "위기를 감지해서"라기보다 **표본이 작아서 생기는 노이즈가 섞여 있을 가능성**이 있습니다. 2009년 이후(월 100건 이상 확보된 이후)의 패턴이 더 신뢰할 만합니다.

---

## 8. 이거 사후변수(look-ahead) 아닌가? — 결측치를 통한 "숨은 연도" 점검

이 프로젝트를 검토하던 중 나온 질문입니다: *"경기순응적으로 인수율이 움직인다는데, 모델에 경기/연도 정보를 어디서 구해다 넣은 거냐?"* — 매우 타당한 의심이라 직접 점검했습니다.

### 8-1. 명시적인 시간 변수는 없음

`config.py`의 `FEATURE_COLS`를 보면 `issue_d`, `issue_month` 같은 날짜/연도 변수는 모델 입력에 **전혀 포함되지 않습니다.** 무위험금리 매칭이나 코호트 분류에는 발행월을 쓰지만, 그건 타깃(ER) 계산과 사후 집계용이지 모델 피처가 아닙니다.

### 8-2. 그런데도 진짜 문제가 있었음 — 결측치를 통한 우회 유출

날짜를 직접 넣지 않았어도, **더 교묘한 경로로 "몇 년도 대출인지"가 새고 있었습니다.** Lending Club은 2015~2016년경부터 신용조회사(credit bureau) 데이터를 훨씬 더 많이 수집하기 시작했습니다. 그 결과 `tot_cur_bal`, `mort_acc`, `num_actv_rev_tl`, `mo_sin_old_rev_tl_op` 등 **변수 87개 중 50개(57%)** 가 2007~2010년 발행 대출에서는 **100% 결측**이고, 2016~2018년 발행 대출에서는 거의 결측이 없습니다 (`결과/era편향_점검.csv`).

LightGBM/XGBoost 같은 트리 모델은 결측값을 "이 값이 없을 때 어느 방향으로 갈지"를 학습해서 자동으로 처리합니다. 즉 모델이 "이 필드들이 몽땅 비어있다 = 오래된 대출"이라는 것을 결측 패턴만으로 알아채고, **날짜를 본 적이 없어도 사실상 "이 대출이 대략 몇 년도인지"를 우회해서 학습했을 가능성**이 있었습니다. 이게 사실이라면, "경기순응적 인수율"은 실제 신용위험 신호가 아니라 "학습 데이터에 있던 해당 연도의 평균 결과를 그대로 재현한 것"에 가까워서, 훨씬 설득력이 떨어지는 결과가 됩니다.

### 8-3. 검증: 그 변수 50개를 다 빼고 다시 학습

era-편향 변수 50개를 전부 제외하고, 모든 연도에 걸쳐 항상 존재하는 **37개 변수만으로** XGBoost를 다시 학습했습니다 (`src/final_model.py`, `config.SAFE_FEATURE_COLS`).

| | 전체 변수(88개) | era-안전 변수만(37개) |
|---|---|---|
| 예측-실제 상관계수 (test) | 0.221 | 0.193 |
| 2008-2009 평균 인수율 | 81.3% | 81.1% |
| 2011-2013 평균 인수율 | 97.4% | 96.9% |

**상관계수는 소폭만 떨어지고(0.221→0.193), 경기순응적 인수율 패턴은 거의 그대로 유지됩니다.** 즉 이 패턴은 "숨은 연도 정보"가 아니라, grade·금리·DTI·연체이력·신용한도 사용률 같은 **실제 대출자 신용 정보**에서 나온 것으로 확인됐습니다. 안전한 편이라는 게 검증된 이 37개 변수 모델을 최종 모델로 채택했고, 아래 9절 결과는 이 모델 기준입니다.

### 8-4. 남은 한계

이번 점검은 "결측치를 통한 우회 유출"이라는 한 가지 경로만 확인한 것입니다. 더 완전히 검증하려면 **시간 기준 분할**(예: 2007~2014년 대출로만 학습하고, 모델이 한 번도 결과를 본 적 없는 2015~2018년 대출로만 평가)을 해보는 게 좋습니다. 지금의 train/test 분할은 무작위 분할이라 두 세트 모두 2007~2018년 전체 시기를 포함하고 있어서, "완전히 새로운 시기에도 통하는가"까지는 아직 확인하지 못했습니다.

---

## 9. 최종 결과 요약 (era-안전 변수 기준)

| 전략 | Sharpe | Sortino | 비고 |
|---|---|---|---|
| 전량 투자 (현행) | 0.79 | 1.53 | 베이스라인 |
| Grade A-C만 | 0.97 | 2.19 | LC 자체 등급 베이스라인 |
| LightGBM top-90% | 0.78 | 1.47 | 교차검증 결과 신뢰 불가 (5절) |
| XGBoost top-45% (그리드서치, era-안전 변수) | 1.56 | 4.28 | 신호는 검증됐으나 K값 자체의 근거는 약함 (7절) |
| **XGBoost, ER_hat>0 (파라미터 없는 규칙, era-안전 변수)** | **1.48** | **5.55** | **최종 추천 — 경제적 근거 있고, era-편향 점검 통과, 성과도 최고** |

**결론**: "모델이 LC 자체 등급을 못 이긴다"는 LightGBM만 봤을 때의 결론은 모델 전반의 한계가 아니라 LightGBM 하이퍼파라미터 및 K 선택 방식의 한계였습니다. XGBoost에 **"예측 초과수익률이 양수면 투자한다"는 파라미터 없는 규칙**을 적용하고, **날짜 정보가 결측치로 우회 유출되지 않는 변수만 사용해도**, 그리드서치보다 더 좋고(Sortino 5.55 > 4.28), 투자 규모도 89%로 훨씬 크며, 근거도 명확한 결과를 얻습니다. **최종 추천: XGBoost(era-안전 변수 37개) + ER_hat>0 규칙.**

---

## 10. 왜 이 규칙이 이 대출을 받아들이는가 — 원인 분석

지금까지는 "ER_hat>0이면 투자한다"는 규칙 자체는 파라미터가 없지만, **그 결과 왜 89%가 통과되는지, 어떤 대출이 걸러지는지**는 설명하지 않았습니다. "그냥 모델 돌렸더니 이 값이 나왔다"에서 한 걸음 더 들어가, 실제로 무엇이 accept/reject를 가르는지 분석했습니다 (`src/explain_model.py`).

### 10-1. 등급(grade)별 인수율 — `시각화/acceptance_by_grade.png`, `결과/등급별_인수율.csv`

| grade | A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|---|
| 달러 인수율 | 97% | 95% | 89% | 82% | 73% | 69% | 58% |

**LC 자체 등급이 낮아질수록(A→G) 인수율이 완벽하게 단조 감소합니다.** 즉 이 모델의 결정은 LC의 기존 위험 등급 체계와 방향이 정확히 일치하면서, 등급 안에서 더 세밀하게(sub_grade 단위로도 같은 패턴, `결과/세부등급별_인수율.csv`) 걸러내고 있다는 뜻입니다 — "등급 체계를 무시하고 엉뚱한 걸 학습한 것"이 아니라 "등급 체계를 정교화한 것"이라는 근거입니다.

### 10-2. 신용변수별 인수율 — `시각화/acceptance_by_feature.png`, `결과/신용변수별_인수율.csv`

주요 신용변수를 5분위로 나눠 각 구간의 인수율을 봤습니다:

- **DTI(부채/소득 비율)**: 낮을수록 인수율 높음 (92% → 81%, 상식과 일치)
- **FICO 점수**: 높을수록 인수율 높음 (83% → 93%, 상식과 일치)
- **금리(int_rate)**: 낮을수록 인수율 높음 (97% → 77%, grade와 같은 방향)
- **신용카드 사용률(revol_util)**: **예상과 반대로, 높을수록 인수율이 높습니다** (82% → 94%). 직관적으로는 신용카드를 많이 쓸수록(사용률이 높을수록) 재정적으로 빠듯하다는 신호라 위험해 보이는데, 모델은 반대로 반응합니다. 이는 다른 변수(예: 소득, grade)를 이미 통제한 상태에서 revol_util이 오히려 "신용을 활발하게 쓰지만 잘 관리하는 사람"의 신호로 작용했을 가능성이 있는데, 정확한 이유는 추가 분석이 필요한 **미해결 이상 현상**으로 남겨둡니다.

### 10-3. SHAP 변수 중요도 — `시각화/shap_importance.png`, `결과/SHAP_변수중요도.csv`

SHAP(개별 대출마다 각 변수가 예측값을 얼마나/어느 방향으로 밀었는지 분해하는 기법)으로 확인한 상위 요인:

1. **int_rate (금리)** — 압도적 1위
2. **sub_grade** — LC 자체 세부등급
3. home_ownership, revol_util, addr_state, inq_last_6mths, dti, term_months, fico_avg 순

**금리와 등급이 SHAP 기준으로도 가장 크게 기여합니다.** 이는 10-1의 등급별 단조 패턴과 정확히 들어맞고, "모델이 실제로는 LC의 자체 가격결정(금리)·등급 정보를 정교하게 재해석해서, 같은 등급/금리 안에서도 DTI·연체이력·신용조회 횟수 같은 보조 신호로 한 번 더 걸러낸다"는 일관된 스토리를 뒷받침합니다.

### 10-4. 정확히 어떤 사람을 거절하는가 — 승인/거절 프로필 직접 비교

`ER_hat`은 "0.3×FICO − 0.2×DTI" 같은 하나의 깔끔한 공식이 아니라, XGBoost가 37개 변수를 수백 개의 나무로 조합해서 뱉어내는 **학습된 함수의 출력값**입니다. 그래서 "정확한 변수 정의"를 한 줄의 수식으로 보여드릴 수는 없지만, **실제로 거절된 대출과 승인된 대출의 프로필을 직접 비교**하면 무엇이 갈리는지 구체적으로 알 수 있습니다 (테스트셋 58.5만 건 중 승인 90.2% / 거절 9.8%).

**등급 구성 자체가 다릅니다** — `시각화/accept_reject_grade_dist.png`, `결과/승인거절_등급구성비.csv`:
- 승인 그룹은 A~C가 81%를 차지
- 거절 그룹은 D~G가 68%를 차지 (그중 C~E가 다수)

**같은 이유로, 신용지표의 중앙값 자체가 다릅니다** — `시각화/accept_reject_profile.png`, `결과/승인거절_프로필비교.csv`:

| 변수 | 승인 중앙값 | 거절 중앙값 | 차이 |
|---|---|---|---|
| 금리(int_rate) | 11.99% | 15.61% | 거절이 +30% 높음 |
| DTI | 17.19 | 22.56 | 거절이 +31% 높음 |
| 연소득(annual_inc) | $65,000 | $55,000 | 거절이 -15% 낮음 |
| 신용거래기간(credit_history_months) | 180개월 | 157개월 | 거절이 -13% 짧음 |
| 최근 6개월 신용조회(inq_last_6mths) | 0회 | 1회 | 거절이 더 많음 |
| FICO 평균 | 692 | 682 | 거절이 -1%(10점) 낮음 |

**한 문장으로 요약하면**: 거절되는 대출은 전형적으로 **"등급이 낮고(D~G), 금리가 높고(15%대+), DTI가 높고(22+), 소득이 상대적으로 적고, 신용거래 이력이 짧고, 최근 신용조회가 잦은"** 대출입니다. 다만 이건 "이 조건이면 무조건 거절"이라는 하드 컷오프가 아니라, **여러 약한 신호가 겹쳐서** 결과적으로 예측 초과수익률을 0 이하로 끌어내리는 것입니다 — 실제로 이 조건 중 일부만 나쁜 대출은 대부분 통과되고(등급별 인수율이 0%나 100%가 아니라 58~97% 사이인 이유), 여러 조건이 동시에 나쁠 때만 거절됩니다.

**단순화된 규칙(대리모델)로 확인해봄**: XGBoost의 판단을 흉내 내는 얕은 의사결정나무를 별도로 학습해봤는데, 균형정확도가 0.67~0.71 정도로 나와서 "대략적인 근사"는 되지만 **XGBoost의 판단을 완전히 대체할 만큼 깔끔한 단일 규칙은 없다**는 것도 확인했습니다. 즉 진짜 정의는 "37개 변수를 반영한 XGBoost 모델 그 자체"이고, 위 표는 그 모델이 실제로 무엇을 근거로 판단하는지에 대한 **가장 정직한 요약**입니다.

### 10-5. 종합 내러티브

K=45%나 89%라는 숫자 자체는 목표가 아니었고, **"이 대출이 국채보다 못 벌 것 같으면 안 빌려준다"는 경제 논리를 적용한 결과**였습니다(7절). 이번 분석은 그 결과가 블랙박스가 아니라, **LC의 기존 등급·금리 체계를 그대로 따르면서 DTI·FICO·연체이력 등으로 한 번 더 정교화한, 설명 가능한 신용판단**이라는 것을 보여줍니다. 유일한 예외(revol_util 역방향)는 정직하게 한계로 남겨뒀습니다.

---

## 11. 세 번째 K 정의 — 주(state) GDP 성장률의 "기울기"로 정하기

7절에서는 K를 그리드서치로 찾는 대신 "ER_hat>0" 규칙으로 대체했습니다. 이번에는 **또 다른 방식**을 시도했습니다: 대출자가 사는 주(state)의 실질 GDP 성장률 추세를 이용해서, 경기가 개선되는 주는 K(투자 비율)를 높이고, 경기가 나빠지는 주는 K를 낮추는 방식입니다.

### 11-1. 데이터: BEA SAGDP11 (주별 실질 GDP 성장률)

미국 경제분석국(BEA)이 API 키 없이 공개하는 벌크 데이터(`apps.bea.gov/regional/zip/SAGDP.zip`)에서 `SAGDP11` 테이블(LineCode=1, "All industry total", 단위 "Percent change")을 받아 `data/raw/SAGDP11_state_real_gdp_growth.csv`로 정리했습니다. 50개 주 + DC, 1998~2025년 연간 실질 GDP 성장률(%)입니다.

### 11-2. "기울기"를 어떻게 정의했는가 — `src/gdp_slope.py`

성장률 자체가 아니라 **성장률의 추세(가속/감속)**를 보기 위해, 각 주·연도마다 **직전 3개년(t-2, t-1, t) 성장률에 대한 선형회귀 기울기**를 계산했습니다. 예를 들어 네바다는 2009년(금융위기) 기울기가 -4.5(성장률이 가파르게 나빠지는 중)였고, 2011년(회복기)에는 +4.65(가파르게 좋아지는 중)였습니다 — 국가 단위로 봤던 경기순응성(7-3절)을 **주 단위로 더 세분화**한 것입니다.

**발표시차 반영**: 연간 GDP 통계는 보통 다음 해 6월경 발표됩니다. 그래서 대출 발행월이 6월 이전이면 "발행연도-2"년 자료까지만, 6월 이후면 "발행연도-1"년 자료까지만 사용하도록 제한했습니다 — 8절에서 겪은 것과 같은 종류의 사후정보 유출을 피하기 위함입니다.

### 11-3. 기울기 → K 변환

계산된 기울기를 **전체 기간의 기울기 분포에서의 백분위**로 바꾸고, 그 백분위를 K=30%(최악의 기울기)~K=100%(최고의 기울기) 구간에 선형으로 매핑했습니다. 즉 경기가 가장 나쁘게 꺾이는 주·시기는 그 지역 대출의 30%만, 가장 좋게 개선되는 주·시기는 100%까지 투자하도록 했습니다. 이후 각 (주, 발표연도) 그룹 안에서 모델이 예측한 초과수익률(ER_hat) 순위 상위 K%를 선택했습니다(`metrics.variable_k_selection` — 기존 `top_k_selection`을 코호트별 고정 K에서 그룹별 가변 K로 일반화한 함수).

**참고**: K_min=30%, K_max=100%라는 구간 자체는 여전히 사람이 정한 값입니다. "그리드서치로 결과를 보고 끼워맞췄다"는 7절의 비판에서는 벗어났지만(경제 논리로 방향을 정했으므로), 구간의 정확한 폭은 하나의 설계 선택이라는 점은 정직하게 밝혀둡니다.

### 11-4. 결과 — `결과/GDP기울기_K_모델별비교표.csv`, `시각화/gdp_slope_k_model_comparison.png`

5개 모델(era-안전 변수 37개로 통일해서 재학습) 모두에 이 방식을 적용한 결과:

| 모델 | 달러인수율 | Sharpe | Sortino |
|---|---|---|---|
| 전량 투자 (베이스라인) | 100% | 0.79 | 1.53 |
| Grade A-C (베이스라인) | 75% | 0.97 | 2.19 |
| Linear Regression | 70% | 1.21 | 3.25 |
| Random Forest | 70% | 1.07 | 2.08 |
| **XGBoost** | 70% | **1.61** | **6.63** |
| CatBoost | 70% | 0.55 | 0.69 |
| LightGBM | 70% | 0.73 | 1.14 |

**XGBoost가 다시 한번 압도적입니다** — Sortino 6.63으로, 7절의 "ER_hat>0" 규칙(5.55)보다도 더 높습니다. Linear Regression도 베이스라인을 넘었지만(3.25), Random Forest(2.08)·CatBoost(0.69)·LightGBM(1.14)은 Grade A-C(2.19)를 못 넘었습니다 — 즉 **"K를 정의하는 방식"과 무관하게 XGBoost가 이 데이터·이 변수 조합에서 일관되게 가장 좋은 모델**이라는 게 세 번째 방법으로도 재확인됐습니다.

**주의**: 이 결과는 아직 5-fold 교차검증을 거치지 않았습니다(6-2절에서 했던 것과 같은 안정성 검증). XGBoost가 여기서도 잘 나온 것은 고무적이지만, 단일 결과라 "K=45%가 validation에서 최고였지만 교차검증하니 노이즈였다"(5절)와 같은 상황이 아닌지는 추가 확인이 필요합니다.

---

## 12. 시각화 파일 설명 — `시각화/` 폴더

### 12-1. `acceptance_by_grade.png` — 등급별 인수율 ⭐ (최종 핵심)
- LC 자체 등급(A~G)별로 ER_hat>0 규칙의 달러 인수율을 막대그래프로: A=97% → G=58%까지 완벽한 단조 감소
- 모델의 판단이 LC 자체 위험등급과 방향이 일치한다는 가장 직접적인 증거 (10-1절)
- 원자료: `결과/등급별_인수율.csv` (세부등급 단위는 `결과/세부등급별_인수율.csv`)

### 12-2. `acceptance_by_feature.png` — 신용변수 5분위별 인수율 ⭐ (최종 핵심)
- DTI/FICO/신용카드 사용률/금리 4개 변수를 5분위로 나눠 각 구간의 인수율을 2x2로 비교
- DTI·FICO·금리는 상식과 일치하는 방향, 신용카드 사용률만 예상과 반대 방향 (10-2절에서 한계로 논의)
- 원자료: `결과/신용변수별_인수율.csv`

### 12-3. `shap_importance.png` — SHAP 변수 중요도 ⭐ (최종 핵심)
- 개별 대출 2만 건 샘플에서 SHAP으로 계산한, 예측 초과수익률에 대한 변수별 평균 기여도
- int_rate(금리), sub_grade가 압도적 1·2위 — 등급별 인수율 패턴(11-1)과 일관된 근거
- 원자료: `결과/SHAP_변수중요도.csv`

### 12-4. `accept_reject_grade_dist.png` — 승인/거절 그룹의 등급 구성비 ⭐ (최종 핵심)
- 승인된 대출과 거절된 대출 각각이 등급 A~G로 어떻게 구성되는지 나란히 비교
- 승인 그룹은 A~C가 81%, 거절 그룹은 D~G가 68% — "정확히 어떤 사람을 거절하는가"에 대한 가장 직접적인 답 (10-4절)
- 원자료: `결과/승인거절_등급구성비.csv`

### 12-5. `accept_reject_profile.png` — 승인 vs 거절 대출의 프로필 차이 ⭐ (최종 핵심)
- 금리·DTI·연소득·신용거래기간 등 주요 변수의 중앙값이 승인 그룹과 거절 그룹에서 몇 % 차이나는지 막대그래프로 비교
- 거절 그룹은 금리 +30%, DTI +31% 높고, 소득 -15%, 신용거래기간 -13% 낮음 — "정확한 변수 정의"에 대한 가장 구체적인 답 (10-4절)
- 원자료: `결과/승인거절_프로필비교.csv`

### 12-6. `threshold_rule_acceptance.png` — XGBoost 'ER_hat>0' 규칙의 월별 투자 인수율
- **x축**: 대출 발행월, **y축**: 그 달에 이 규칙으로 실제 투자된 비율(달러 기준) — era-안전 변수 모델 기준
- 위기 구간(주황 음영, 2008~2009)은 낮고 호황 구간(초록 음영, 2011~2013)은 높음 — 규칙이 경기에 따라 자동으로 인수율을 조절한다는 증거
- 2007~2008년 초의 급격한 등락은 표본 수가 워낙 적어(월 12~173건) 생기는 노이즈일 수 있음 (7-3절 참고)
- 원자료: `결과/ER_hat규칙_월별인수율.csv`

### 12-7. `final_rule_comparison.png` — 최종 비교: 그리드서치 K vs 파라미터 없는 규칙
- 베이스라인 2개 + XGBoost의 두 가지 선택 방식(그리드서치 K=45% / ER_hat>0)을 나란히 비교 (era-안전 변수 모델 기준)
- ER_hat>0 규칙이 Sortino 기준으로 가장 높음 — 이 프로젝트의 최종 결론 그래프
- 원자료: `결과/최종규칙_비교표.csv`

### 12-8. `model_comparison.png` — 베이스라인 vs 5개 모델 비교 (전체 변수, 그리드서치 K 기준)
- 전량투자·Grade A-C 베이스라인 2개 + 5개 모델(각자 validation split에서 찾은 최적 K 적용)의 Sharpe/Sortino 비교
- XGBoost가 가장 높았고, 이 발견이 7~9절의 파라미터 없는 규칙 + era-편향 점검으로 이어짐. era-안전 변수만 썼을 때의 최종 수치는 9절 표 참고
- 원자료: `결과/모델별_비교표.csv`

### 12-9. `cv_instability_xgb.png` — XGBoost: K값별 Sortino의 fold간 안정성 (5-fold 교차검증, 전체 변수)
- 회색 얇은 선 5개 = 5번의 교차검증 fold 각각의 K-Sortino 곡선, 굵은 파란선 = 평균, 주황 점선 = Grade A-C 베이스라인
- K=20~65% 구간에서 평균이 일관되게 베이스라인 위에 있음 → XGBoost의 신호 자체는 신뢰할 만하다는 근거 (다만 "K값 자체"의 근거는 7절, era-편향 여부는 8절에서 별도로 다룸)
- 원자료: `결과/XGBoost_K값별_성과_교차검증.csv`, fold별 원본은 `outputs/cv_fold_curves_xgb.csv`

### 12-10. `cohort_excess_return.png` — 코호트(발행월)별 초과수익률 추이 (LightGBM 1차 결과)
- **x축**: 대출 발행월(2007~2018), **y축**: 그 달 대출들의 달러가중 연환산 초과수익률(ER), 6개월 이동평균
- 2008~2009년 마이너스, 2011~2013년 최고(+5%대), 이후 완만히 하락 — 이 오르내림의 폭이 Sharpe/Sortino의 "위험"(분모)입니다
- 원자료: `결과/코호트별_초과수익률_ER.csv`

### 12-11. `strategy_comparison.png`, `k_sweep.png`, `cv_instability.png` — LightGBM 1차 분석 자료
- 최초 LightGBM 단일모델 분석 당시의 전략 비교, K-스윕, 교차검증 그래프 (5절 참고용, 최종 결론은 9절로 대체됨)

---

## 13. 결과 CSV 파일 설명 — `결과/` 폴더

| 파일 | 내용 |
|---|---|
| `최종규칙_비교표.csv` | 최종 결론 표 (era-안전 변수 기준, 9절과 동일) ⭐ |
| `ER_hat규칙_월별인수율.csv` | ER_hat>0 규칙의 발행월별 달러 인수율 (era-안전 변수 기준) ⭐ |
| `GDP기울기_K_모델별비교표.csv` | 주 GDP 성장률 기울기 기반 K를 5개 모델에 적용한 결과 (11절 근거자료) ⭐ |
| `등급별_인수율.csv`, `세부등급별_인수율.csv` | grade/sub_grade별 달러 인수율 (10-1절 근거자료) ⭐ |
| `신용변수별_인수율.csv` | DTI/FICO/신용카드 사용률/금리 5분위별 달러 인수율 (10-2절 근거자료) ⭐ |
| `SHAP_변수중요도.csv` | 변수별 평균 |SHAP| 값 — accept/reject 판단 기여도 순위 (10-3절 근거자료) ⭐ |
| `승인거절_등급구성비.csv` | 승인/거절 그룹 각각의 grade 구성 비율 (10-4절 근거자료) ⭐ |
| `승인거절_프로필비교.csv` | 승인/거절 그룹의 금리·DTI·소득 등 주요 변수 중앙값 비교 (10-4절 근거자료) ⭐ |
| `era편향_점검.csv` | 변수 87개 각각의 2007-2010 vs 2016-2018 결측률과 차이, era-편향 여부 (8절 근거자료) ⭐ |
| `모델별_비교표.csv` | 5개 모델의 상관계수·그리드서치 K·test Sharpe/Sortino + 베이스라인 2종 (전체 변수 기준) |
| `XGBoost_K값별_성과_교차검증.csv` | XGBoost 5-fold 교차검증 K별 평균 Sortino와 fold간 표준편차 (전체 변수 기준) |
| `전략별_비교표.csv` | (1차 결과) LightGBM 기준 4가지 전략 비교 |
| `K값별_성과_테스트셋.csv` | (1차 결과) LightGBM, 테스트셋에서 K별 Sharpe/Sortino |
| `K값별_성과_교차검증.csv` | (1차 결과) LightGBM 5-fold 교차검증 K별 평균 Sortino |
| `코호트별_초과수익률_ER.csv` | 발행월별 초과수익률 원자료 (LightGBM 기준) |

---

## 14. 한계 및 향후 개선 방향

1. **시간 기준 분할 검증이 아직 없음** (8-4절) — 지금은 무작위 train/test 분할이라, 모델이 완전히 처음 보는 미래 시기에도 통하는지는 아직 확인 못 함. 2007~2014년 학습 / 2015~2018년 평가 같은 walk-forward 검증을 추가하면 가장 확실함
2. **GDP 기울기 기반 K(11절)는 아직 교차검증을 안 거침** — XGBoost가 6-2절처럼 안정적인 신호인지, 아니면 5절의 LightGBM K=90%처럼 단일 결과의 노이즈인지 추가 확인 필요
3. **신용카드 사용률(revol_util)이 예상과 반대 방향으로 작용함** (10-2절) — 다른 변수와의 상호작용 때문으로 추정되나 원인이 명확히 규명되지 않음
4. **Random Forest, Linear Regression도 era-편향 점검 + ER_hat>0 규칙 + SHAP 분석으로 재검증 필요** — 지금은 XGBoost만 확인함
5. **2007~2008년 초 데이터는 표본이 작아 노이즈가 섞여 있을 수 있음** (7-3절)
6. **CatBoost는 하이퍼파라미터 튜닝을 더 해볼 여지가 있음**
7. **분석 기간이 2007~2018로 제한됨** — 데이터 마감 시점(2020-12) 때문에 만기가 다 지난 vintage만 썼습니다.
8. **GDP 기울기→K 변환의 K_min/K_max(30%~100%), 3년 트레일링 윈도, 6월 발표시차 컷오프는 사람이 정한 설계값** (11-3절) — 경제 논리로 방향은 잡았지만 정확한 폭은 조정 여지가 있음
9. **다음으로 시도해볼 만한 방향**: XGBoost 하이퍼파라미터 추가 튜닝, grade 내부에서의 잔차 스코어링, 회귀 대신 분류(부도확률) 방식과의 비교, ER_hat 임계값을 0이 아닌 다른 경제적 기준(예: 목표 수익률)으로 바꿔보기

---

## 15. 전체 폴더 구조

```
렌딩클럽 version 2/
├── 가이드문서/         ← 이 문서
│   └── 가이드.md
├── 결과/               ← 요약 결과 표 (CSV)
│   ├── 최종규칙_비교표.csv                  ⭐ 최종 결론
│   ├── ER_hat규칙_월별인수율.csv            ⭐ 최종 결론
│   ├── GDP기울기_K_모델별비교표.csv         ⭐ 세 번째 K 정의 (11절)
│   ├── 등급별_인수율.csv, 세부등급별_인수율.csv  ⭐ 원인 분석
│   ├── 신용변수별_인수율.csv                ⭐ 원인 분석
│   ├── SHAP_변수중요도.csv                  ⭐ 원인 분석
│   ├── 승인거절_등급구성비.csv              ⭐ 원인 분석 (정확한 변수 정의)
│   ├── 승인거절_프로필비교.csv              ⭐ 원인 분석 (정확한 변수 정의)
│   ├── era편향_점검.csv                     ⭐ 사후변수/결측치 우회 유출 점검
│   ├── 모델별_비교표.csv                    5개 모델 비교
│   ├── XGBoost_K값별_성과_교차검증.csv      XGBoost 교차검증 상세
│   ├── 전략별_비교표.csv                    (1차, LightGBM)
│   ├── K값별_성과_테스트셋.csv              (1차, LightGBM)
│   ├── K값별_성과_교차검증.csv              (1차, LightGBM)
│   └── 코호트별_초과수익률_ER.csv
├── 시각화/             ← 그래프 (PNG)
│   ├── acceptance_by_grade.png               ⭐ 원인 분석
│   ├── acceptance_by_feature.png             ⭐ 원인 분석
│   ├── shap_importance.png                   ⭐ 원인 분석
│   ├── accept_reject_grade_dist.png          ⭐ 원인 분석 (정확한 변수 정의)
│   ├── accept_reject_profile.png             ⭐ 원인 분석 (정확한 변수 정의)
│   ├── gdp_slope_k_model_comparison.png      ⭐ 세 번째 K 정의 (11절)
│   ├── threshold_rule_acceptance.png         ⭐ 최종 결론
│   ├── final_rule_comparison.png             ⭐ 최종 결론
│   ├── model_comparison.png                  5개 모델 비교
│   ├── cv_instability_xgb.png                XGBoost 교차검증
│   ├── cohort_excess_return.png              (1차, LightGBM)
│   ├── strategy_comparison.png                (1차, LightGBM)
│   ├── k_sweep.png                           (1차, LightGBM)
│   └── cv_instability.png                    (1차, LightGBM)
├── src/                ← 실제 분석/모델링 코드
│   ├── compare_models.py      ← 5개 모델 비교
│   ├── cross_validate_xgb.py  ← XGBoost 교차검증
│   ├── final_model.py         ← 최종 모델 (era-안전 변수 + ER_hat>0 규칙 검증)
│   ├── explain_model.py       ← 등급/신용변수별 인수율 + SHAP 원인 분석
│   ├── gdp_slope.py            ← 주 GDP 성장률 기울기 → K 변환 (11절)
│   ├── gdp_slope_k_compare.py  ← GDP 기울기 기반 K를 5개 모델에 적용
│   └── config.py              ← SAFE_FEATURE_COLS: era-편향 없는 37개 변수 목록
├── models/             ← 학습된 모델 파일
│   ├── xgb_er_model_safe.json  ⭐ 최종 모델 (era-안전 변수 37개)
│   └── xgb_er_model.json       (참고용, 전체 변수 88개 버전)
├── outputs/            ← 코드 실행 시 생성되는 원본 산출물 (위 결과/시각화의 원본)
└── data/raw/           ← 원본 데이터 (SAGDP11_state_real_gdp_growth.csv 포함)
```
